# resources/themes/

Empty. Color themes are meaningless without a renderer to apply them —
see `docs/renderer.md` and `docs/status.md` for why no GUI/renderer
exists yet in this build. `TerminalConfig` already persists a
`theme_name` setting (`docs/configuration.md`) for whenever a renderer
exists to look one up here.
