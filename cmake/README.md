# cmake/

Empty for now. This is where CMake helper modules (find-package scripts,
compiler-warning presets shared across targets, etc.) would live once
`CMakeLists.txt` grows past what fits comfortably in one file.
`CMakeLists.txt` currently doesn't need any — see `docs/build.md`.
